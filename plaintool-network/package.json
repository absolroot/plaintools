{
  "name": "plaintool-network",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.0.0",
  "engines": {
    "node": ">=22.19 <23"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @plaintool/web",
    "build": "npm run seo:check && npm run ui:check && node scripts/build-site.mjs --target preview && npm run qa:network -- --target preview",
    "build:production": "npm run seo:check:production && npm run ui:check && node scripts/validate-production-config.mjs && node scripts/build-site.mjs --target production && npm run qa:network -- --target production",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run seo:check && npm run ui:check && npm run type:check && npm run lint && npm run format:check",
    "type:check": "tsc -p tsconfig.json && npm run check --workspace @plaintool/web",
    "lint": "eslint .",
    "format:check": "prettier --check \"apps/web/src/**/*.{astro,css,js,ts}\" \"packages/*/src/**/*.ts\" \"scripts/**/*.{js,mjs}\" \"*.{js,json}\"",
    "format": "prettier --write \"apps/web/src/**/*.{astro,css,js,ts}\" \"packages/*/src/**/*.ts\" \"scripts/**/*.{js,mjs}\" \"*.{js,json}\"",
    "verify": "npm run test && npm run check && node scripts/build-site.mjs --target preview && npm run qa:network -- --target preview",
    "ui:check": "node scripts/check-ui-detail.mjs",
    "ui:qa": "python scripts/qa-redesign.py",
    "qa:network": "node scripts/qa-network.mjs",
    "locale:check": "node scripts/check-locale-reviews.mjs && node scripts/test-locale-review-gate.mjs",
    "locale:check:production": "node scripts/check-locale-reviews.mjs --production && node scripts/test-locale-review-gate.mjs",
    "seo:check": "npm run locale:check && node scripts/check-seo-registry.mjs && node scripts/test-seo-registry-gate.mjs",
    "seo:check:production": "npm run locale:check:production && node scripts/check-seo-registry.mjs --production && node scripts/test-seo-registry-gate.mjs"
  },
  "devDependencies": {
    "@eslint/js": "9.35.0",
    "eslint": "9.35.0",
    "eslint-plugin-astro": "1.3.1",
    "globals": "16.3.0",
    "prettier": "3.6.2",
    "prettier-plugin-astro": "0.14.1",
    "typescript": "^5.9.2",
    "typescript-eslint": "8.42.0",
    "vitest": "^3.2.4"
  }
}
