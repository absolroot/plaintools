---
import { locales } from "../../../lib/site";

export function getStaticPaths() {
  return locales.map((locale) => ({ params: { locale }, props: { locale } }));
}

return Astro.redirect(
  `/${Astro.params.locale}/invisible-character-remover/`,
  301,
);
---
