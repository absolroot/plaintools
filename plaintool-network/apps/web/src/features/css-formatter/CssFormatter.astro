---
import ConverterStatus from "../../components/ConverterStatus.astro";
import FormatterOptions from "../../components/FormatterOptions.astro";
import FormatterWorkspace from "../../components/FormatterWorkspace.astro";
import Tooltip from "../../components/Tooltip.astro";
import type { ToolHelpCopy } from "../../lib/locale-data/tool-help";
import type { ToolCommonCopy } from "../common-copy-contract";
import type { CssFormatterCopy } from "./contract";
import { createCssFormatterClientCopy } from "./copy";

interface Props {
  copy: CssFormatterCopy;
  commonCopy: ToolCommonCopy;
  help: ToolHelpCopy;
}

const { copy, commonCopy, help } = Astro.props;
const clientCopy = createCssFormatterClientCopy(copy, commonCopy);
---

<section
  class="converter css-formatter"
  data-css-formatter
  aria-label={copy.ariaLabel}
>
  <FormatterWorkspace
    inputId="css-formatter-input"
    outputId="css-formatter-output"
    inputLabel={copy.inputLabel}
    outputLabel={copy.outputLabel}
    inputPlaceholder={copy.inputPlaceholder}
    outputPlaceholder={copy.outputPlaceholder}
    loadSample={copy.loadSample}
    openFile={copy.openFile}
    clear={commonCopy.clear}
    copy={commonCopy.copy}
    download={commonCopy.download}
    fileAccept="text/css,.css"
    staleNotice={copy.outdated}
  />
  <ConverterStatus status={commonCopy.ready}>
    <button
      class="secondary-button formatter-manual-run"
      type="button"
      data-manual-run
      aria-keyshortcuts="Control+Enter Meta+Enter"
      hidden>{copy.format}</button
    >
  </ConverterStatus>
  <FormatterOptions label={copy.optionsLabel}>
    <div class="formatter-options-grid">
      <div class="formatter-option-field">
        <label for="css-indent">{copy.indentationLabel}</label>
        <select id="css-indent" data-indent>
          <option value="2">{copy.twoSpaces}</option>
          <option value="4">{copy.fourSpaces}</option>
          <option value="tab">{copy.tabs}</option>
        </select>
      </div>
      <div class="formatter-option-field">
        <Tooltip
          id="css-print-width-help"
          triggerLabel={help.triggerLabel}
          variant="label"
          mobileAlign="end"
        >
          <label slot="target" for="css-print-width"
            >{copy.printWidthLabel}</label
          >
          {help.formatter.printWidth}
        </Tooltip>
        <input
          id="css-print-width"
          data-print-width
          type="number"
          min="40"
          max="240"
          value="80"
        />
      </div>
    </div>
  </FormatterOptions>
  <p class="formatter-scope-notice" role="note">{copy.scopeNotice}</p>
  <script
    is:inline
    type="application/json"
    data-client-copy
    set:html={JSON.stringify(clientCopy).replace(/</gu, "\\u003c")}
  />
</section>
<script>
  import "./client";
</script>
