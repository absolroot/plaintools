---
import ToolDirectoryCard from "./ToolDirectoryCard.astro";
import type { ToolCatalogItem } from "../lib/tool-catalog";
import type { Locale } from "../lib/site";

interface Props {
  title: string;
  tools: ToolCatalogItem[];
  locale: Locale;
}

const { title, tools, locale } = Astro.props;
---

<section class="directory-category" data-directory-search-category>
  <div class="directory-category-heading">
    <h2>{title}</h2>
    <span aria-hidden="true" data-directory-search-category-count>
      {String(tools.length).padStart(2, "0")}
    </span>
  </div>
  <div class="tool-directory-grid">
    {tools.map((tool) => <ToolDirectoryCard tool={tool} locale={locale} />)}
  </div>
</section>
